#include <iostream>
#include <cmath>
#include <math.h>
int main() {
    system("chcp 65001");


    int y;
    const int x = 2.1;
    const int p = 2;
    int a;
    int b;
    y = std::pow(a, 3) / pow(b, 2);
    a = exp(sqrt(fabs(x)));
    b = ((sin(pow(p, 2))) + pow(x, 3));
    std::cout << y << std::endl;
    return 0;
}

